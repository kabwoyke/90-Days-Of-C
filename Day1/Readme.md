# Compiling a c programming
- Exploring the compilation model of the c compiler i.e gcc

# Example
- Adding a warning flag in compilation -> gcc -Wall main.c -o main
- Generating assemply file and pre-processing file(.i) -> gcc -Wall -save-temps main.c main